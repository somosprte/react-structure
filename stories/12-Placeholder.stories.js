import React from 'react';

import { Placeholder } from 'components';

export default {
  title: 'Layout|Placeholder',
};

export const repository = () => <Placeholder ready={false} component={Placeholder.Repository} />;
